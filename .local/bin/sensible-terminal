#!/usr/bin/env zsh
if [[ "${ZSH_EVAL_CONTEXT}" != "toplevel" ]]; then
    return 1
fi

declare -ar emulators=(
    terminator
    tilda
)

function main() {
    for emulator in "${emulators[@]}"; do
        exe="$(command -v "${emulator}")" && exec "${exe}"
    done
}

main "${@}"
